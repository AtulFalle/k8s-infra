apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-app-stage
  namespace: argocd
  labels:
    app.kubernetes.io/name: demo-app
    app.kubernetes.io/instance: demo-app-stage
    environment: stage
  finalizers:
    - resources-finalizer.argocd.argoproj.io/cascade
spec:
  project: workloads
  source:
    repoURL: https://github.com/AtulFalle/k8s-infra.git
    targetRevision: HEAD
    path: charts/app
    helm:
      releaseName: demo-app-stage
      valueFiles:
        - ../../envs/stage/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
