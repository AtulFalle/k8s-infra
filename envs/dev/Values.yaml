replicaCount: 1

image:
  repository: nginx
  tag: "1.27"

service:
  port: 80
  targetPort: 80

ingress:
  enabled: true
  host: demo-dev.local
vault:
  path: secret
  role: vault-dev

secrets:
  enabled: true
  vaultPath: dev/app
  vaultRole: dev
  serviceAccountName: vault-dev
