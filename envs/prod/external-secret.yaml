kind: ExternalSecret
apiVersion: external-secrets.io/v1
metadata:
  name: app-secrets
  namespace: prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: SecretStore
  target:
    name: app-secrets
  data:
    - secretKey: DB_HOST
      remoteRef:
        key: prod/app
        property: DB_HOST

    - secretKey: DB_NAME
      remoteRef:
        key: prod/app
        property: DB_NAME

    - secretKey: DB_USER
      remoteRef:
        key: prod/app
        property: DB_USER

    - secretKey: DB_PASSWORD
      remoteRef:
        key: prod/app
        property: DB_PASSWORD